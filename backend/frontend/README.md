# Frontend Ready

This frontend is prepared for the Book Library project.

## Run
```bash
npm install
npm start
```

## Backend URL
The frontend expects the Django backend at:

- `http://127.0.0.1:8000`

If needed, change the API base URL in:

- `src/app/core/services/api.service.ts`
